# ลิงก์ประจำสาขา (ใช้ภายในทีมเท่านั้น)

เอกสารนี้ไม่ได้ผูกอยู่ในหน้าเว็บไหนเลย และไม่มีหน้าไหนลิงก์มาถึงมันได้
เปิดจากที่นี่ตรง ๆ ตอนจะคัดลอกลิงก์ไปส่งลูกค้า **อย่าพิมพ์ URL เอง** ให้ก๊อปจากตารางนี้เท่านั้น
เพื่อกันพิมพ์ผิดสาขา (`bangna` ↔ `rangsit` ↔ `vibhavadi` หน้าตาคล้ายกันมาก)

## ตารางลิงก์

| สาขา | โฟลเดอร์ | URL ที่ส่งลูกค้า | สถานะ |
|---|---|---|---|
| บางนา (Bangna) | `/bangna/` | https://lioladex.github.io/galaxy-class-schedule/bangna/ | ✅ ใช้งานจริง |
| วิภาวดี (Vibhavadi) | `/vibhavadi/` | https://lioladex.github.io/galaxy-class-schedule/vibhavadi/ | ✅ ใช้งานจริง |
| สาทร (Sathorn) | `/sathorn/` | https://lioladex.github.io/galaxy-class-schedule/sathorn/ | ✅ ใช้งานจริง |
| รังสิต (Rangsit) | `/rangsit/` | https://lioladex.github.io/galaxy-class-schedule/rangsit/ | ยังไม่สร้าง |

## เช็กก่อนส่งให้ลูกค้าทุกครั้ง

- [ ] เปิดลิงก์จริงในเบราว์เซอร์ก่อนส่ง — อย่าเชื่อจากการอ่านตัวอักษร ให้ดูป้าย **"📍 สาขา..."** ใต้โลโก้ในหน้าที่เปิดขึ้นมาว่าตรงกับที่ตั้งใจ
- [ ] เทียบชื่อสาขาในแชทที่คุยกับลูกค้า กับป้ายสาขาที่เห็นในหน้าเว็บ ให้ตรงกันคำต่อคำ
- [ ] ถ้าไม่แน่ใจ เปิดเอกสารนี้แล้วก๊อป URL จากตารางด้านบนใหม่ อย่าแก้จากลิงก์เก่าที่เคยส่งไปสาขาอื่น

## ทำไมแต่ละสาขาถึงแยกกันชัดเจน

- แต่ละสาขาเป็นไฟล์ `index.html` คนละไฟล์ ไม่มีหน้ารวมหรือลิงก์เชื่อมข้ามสาขา
- แต่ละหน้ามีป้าย "📍 สาขา..." ต่อว่ากันไม่ให้เผลอ, `<title>` แท็บเบราว์เซอร์ก็บอกชื่อสาขาไว้แล้ว
- แต่ละสาขามีสีเน้นของตัวเอง (ปรับได้ที่ `--plasma` / `--ion` ในไฟล์นั้น ๆ) เพื่อให้ดูออกได้จากภาพรวมโดยไม่ต้องอ่านตัวหนังสือก่อน
- ทุกหน้าใส่ `noindex` และมี `robots.txt` กัน Google เก็บลิงก์ไปแสดงในผลค้นหา ลดโอกาสที่ลูกค้าจะเจอสาขาอื่นเองจากการค้นหา

## เพิ่มสาขาใหม่

1. ก๊อปโฟลเดอร์ `bangna/` ทั้งโฟลเดอร์ ตั้งชื่อใหม่ตามสาขา (ตัวพิมพ์เล็ก ไม่มีเว้นวรรค เช่น `rangsit`)
2. ในไฟล์ `index.html` ของโฟลเดอร์ใหม่ แก้ 4 จุด:
   - `<title>` และแท็ก `og:title` / `og:description` ใน `<head>`
   - `const BRANCH = { key:'...', name:'สาขา...', nameEn:'...' }`
   - `--plasma` / `--ion` ให้เป็นสีที่ไม่ซ้ำสาขาอื่น
   - ตารางคลาสในบล็อก `const STUDIOS = {` ให้ตรงกับสาขานั้น
3. สร้างรูปพรีวิวลิงก์ (ขึ้นตอนแชร์ลิงก์ใน LINE/Messenger ฯลฯ):
   - ก๊อป `assets/og/bangna.svg`-สไตล์ (ดูสคริปต์ gen_og ที่ใช้สร้างของเดิม) เปลี่ยนสี/ชื่อสาขาเป็นภาษาอังกฤษ (`nameEn`) — **อย่าใช้ชื่อไทยในรูปนี้** เพราะตัวแปลง SVG→PNG ที่ใช้ไม่มีฟอนต์ไทย จะกลายเป็นกล่องสี่เหลี่ยม
   - แปลงเป็น `.jpg` ขนาด 1200×630 วางที่ `assets/og/<สาขา>.jpg`
   - อัปเดตแท็ก `og:image` / `twitter:image` ใน `<head>` ให้เป็น URL เต็ม (ไม่ใช่ path สัมพัทธ์) ชี้ไปไฟล์นี้ เช่น `https://lioladex.github.io/galaxy-class-schedule/assets/og/<สาขา>.jpg`
4. เพิ่มแถวในตารางลิงก์ด้านบนของเอกสารนี้
5. เช็กลิสต์ก่อนส่งให้ลูกค้าเหมือนเดิม
